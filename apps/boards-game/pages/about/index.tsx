import styles from './index.module.css';
import { Snake } from '@boards-game/snake';

/* eslint-disable-next-line */
export interface AboutProps {}

export function About(props: AboutProps) {
  return (
    <Snake/>
  );
}

export default About;
